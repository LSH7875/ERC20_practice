//require는 입력값, 컨트랙트 상태 변수, 다른 함수의 반환값이 유효한지를 검사하거나 조건을 만족하는지 검사할 때 사용한다. 
//assert함수는 내부 에러를 테스트하기 위한 용도로 사용한다. 
//assert를 잘 사용하면 분석 도구들이 컨트랙트 코드를 검사해서 함수 호출을 평가하고 assert문이 실패하는 조건을 찾아낼 수 있다. 
//컨트랙트가 잘 작동하도록 작성하면 어떤 경우에도 실패하는 assert문이 발생하지 않는다. 실패하는 assert문이 잇다는 것은 컨트랙트에 버그가 있다는 의미.


